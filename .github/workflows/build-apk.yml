name: Build GigClaw APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:  # Allows manual trigger

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # Needed for creating releases
      
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        
    - name: Setup Android SDK
      uses: android-actions/setup-android@v3
      
    - name: Cache Gradle packages
      uses: actions/cache@v4
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-
          
    - name: Install Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Prebuild Android
      run: npx expo prebuild --platform android --clean
      
    - name: Make gradlew executable
      run: chmod +x android/gradlew
      
    - name: Build Release APK
      run: |
        cd android
        ./gradlew assembleRelease --no-daemon --stacktrace
      env:
        ANDROID_HOME: ${{ env.ANDROID_HOME }}
        
    - name: Check APK exists
      run: |
        echo "Checking for APK..."
        find android -name "*.apk" -type f
        ls -la android/app/build/outputs/apk/release/ || echo "Release directory not found"
        ls -la android/app/build/outputs/apk/debug/ || echo "Debug directory not found"
        
    - name: Upload APK to Artifacts
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: gigclaw-apk
        path: |
          android/app/build/outputs/apk/**/*.apk
        retention-days: 30
        if-no-files-found: warn
        
    - name: Create Release
      if: github.ref == 'refs/heads/main' && github.event_name == 'push' && success()
      uses: softprops/action-gh-release@v2
      with:
        tag_name: v${{ github.run_number }}
        name: GigClaw Release v${{ github.run_number }}
        body: |
          ## GigClaw APK Build
          
          **Build Date:** ${{ github.event.head_commit.timestamp }}
          **Commit:** ${{ github.sha }}
          
          ### Features
          - Auto-accept gig orders
          - Geofence & destination mode
          - Smart batching (max 3 orders)
          - Weekly schedule & zones
          - Anti-detection guardrails
          
          ### Installation
          1. Download APK from Assets below
          2. Enable "Install from unknown sources" on your Android phone
          3. Install APK
          4. Grant permissions (see PERMISSIONS-CHECKLIST.md)
          
          ### Note
          This is an **unsigned APK**. You may need to allow unsigned apps in your Android settings.
        files: android/app/build/outputs/apk/release/*.apk
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
